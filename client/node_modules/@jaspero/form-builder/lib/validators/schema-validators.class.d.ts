import { AbstractControl } from '@angular/forms';
export declare class SchemaValidators {
    static multipleOf(num: number): (control: AbstractControl) => {
        multipleOf: number;
    } | null;
}
