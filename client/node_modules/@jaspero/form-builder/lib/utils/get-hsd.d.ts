import { Observable } from 'rxjs';
import { FieldData } from '../interfaces/field-data.interface';
export interface HSD {
    type?: 'html' | 'static' | 'dynamic';
    value: string;
}
export declare function getHsd(key: string, data: FieldData): Observable<string>;
